mutation RemoveFromCart($input: RemoveCartItemInput!) {
	removeItem(input: $input) {
		id
		totalItems
		totalUniqueItems
		subTotal {
			formatted
		}
		items {
			id
			lineTotal {
				formatted
			}
			unitTotal {
				formatted
			}
		}
	}
}
