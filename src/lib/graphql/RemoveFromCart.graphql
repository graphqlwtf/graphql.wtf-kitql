mutation RemoveFromCart($input: RemoveCartItemInput!) {
  removeItem(input: $input) {
    id
    totalItems
    totalUniqueItems
    subTotal {
      formatted
    }
    items {
      id
      name
      images
      lineTotal {
        formatted
      }
      unitTotal {
        formatted
      }
    }
  }
}
